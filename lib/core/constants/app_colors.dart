import 'package:flutter/material.dart';

// Kelas ini berisi semua warna yang akan kita gunakan di dalam aplikasi.
// Disesuaikan dengan desain UI baru.
class AppColors {
  // Warna Utama dari Desain Baru
  static const Color primaryGreen = Color(0xFF009C22);
  static const Color buttonBlue = Color(0xFF190482);
  static const Color linkBlue = Color(0xFF0057FF);
  static const Color accent = Color(0xFFFFC107);

  // Warna Netral
  static const Color black = Color(0xFF212121);
  static const Color grey = Color(0xFF757575);
  static const Color lightGrey = Color(0xFFE0E0E0);
  static const Color formBackground = Color(
    0xFFF5F5F5,
  ); // Warna abu-abu untuk card
  static const Color white = Color(0xFFFFFFFF);

  // Warna Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA000);

  static var primary;

  // static var accent;
}
